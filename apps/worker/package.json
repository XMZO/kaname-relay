{
  "name": "@kaname-relay/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist --config wrangler.toml"
  },
  "dependencies": {
    "@kaname-relay/core": "workspace:*",
    "@kaname-relay/notifiers": "workspace:*",
    "@kaname-relay/store": "workspace:*",
    "hono": "^4.12.25"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.11.1"
  }
}
